import { AuthPanel } from "./auth-panel";
import { AuthPanelTemplate as template } from "./auth-panel.template";
import { AuthPanelStyles as styles } from "./auth-panel.styles";

AuthPanel.define({
  name: "auth-panel",
  template,
  styles,
});
