import { Contracts } from "./contracts";
import { ContractsTemplate as template } from "./contracts.template";
import { ContractsStyles as styles } from "./contracts.styles";

Contracts.define({
  name: "contract-form",
  template,
  styles,
});
