import { css } from "@microsoft/fast-element";

export const AuthPanelStyles = css`
  form {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }
  input {
    padding: 0.5rem;
    font-size: 1rem;
  }
  button {
    padding: 0.5rem;
    background: #0d6efd;
    color: white;
    border: none;
    cursor: pointer;
  }
`;
