import { SignaturePanel } from "./signature-panel";
import { SignaturePanelTemplate as template } from "./signature-panel.template";
import { SignaturePanelStyles as styles } from "./signature-panel.styles";

SignaturePanel.define({
  name: "signature-panel",
  template,
  styles,
});
