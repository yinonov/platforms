import { CampaignsListView } from "./campaigns-list-view";
import { CampaignsListViewTemplate as template } from "./campaigns-list-view.template";

CampaignsListView.define({
  name: "campaigns-list-view",
  template,
});
